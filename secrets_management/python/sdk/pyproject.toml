[project]
name = "secrets-management-python-sdk"
version = "0"
description = "Dapr secrets management quickstart (Python SDK)."
requires-python = ">=3.11"
dependencies = [
    "dapr>=1.18.0",
    "typing-extensions",
]

[tool.uv]
package = false
