[project]
name = "state-management-python-http"
version = "0"
description = "Dapr state management quickstart (Python HTTP)."
requires-python = ">=3.11"
dependencies = [
    "requests",
    "urllib3>=2.2.2",
]

[tool.uv]
package = false
