"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var BufferSerializer = /** @class */ (function () {
    function BufferSerializer() {
    }
    // constructor() {
    // }
    BufferSerializer.prototype.deserialize = function (data) {
        var deserializedBody = (data === null || data === void 0 ? void 0 : data.toString()) || "";
        // Try to parse it to an object
        // this way manager.invoke has string | object
        try {
            deserializedBody = JSON.parse(deserializedBody);
        }
        catch (e) {
            // Do nothing
        }
        return deserializedBody;
    };
    BufferSerializer.prototype.serialize = function (data) {
        var dataAsString;
        if (typeof data === "object") {
            dataAsString = JSON.stringify(data);
        }
        else {
            dataAsString = (data === null || data === void 0 ? void 0 : data.toString()) || "";
        }
        return Buffer.from(dataAsString);
    };
    return BufferSerializer;
}());
exports.default = BufferSerializer;
