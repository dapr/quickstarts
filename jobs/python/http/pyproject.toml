[project]
name = "jobs-python-http"
version = "0"
description = "Dapr jobs API quickstart (Python HTTP)."
requires-python = ">=3.11"
dependencies = [
    "requests==2.31.0",
]

[tool.uv]
package = false
