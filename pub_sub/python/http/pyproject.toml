[project]
name = "pub-sub-python-http"
version = "0"
description = "Dapr pub/sub quickstart (Python HTTP)."
requires-python = ">=3.11"

[tool.uv]
package = false

[tool.uv.workspace]
members = ["checkout", "order-processor"]
