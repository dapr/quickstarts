[project]
name = "order-processor-fastapi"
version = "0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "dapr-ext-fastapi>=1.18.0",
    "cloudevents>=2.0.0",
    "uvicorn",
    "anyio>=4.4.0",
]

[tool.uv]
package = false
