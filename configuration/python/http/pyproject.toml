[project]
name = "configuration-python-http"
version = "0"
description = "Dapr configuration API quickstart (Python HTTP)."
requires-python = ">=3.11"
dependencies = [
    "Flask",
    "requests",
    "uvicorn",
    "urllib3>=2.2.2",
]

[tool.uv]
package = false
