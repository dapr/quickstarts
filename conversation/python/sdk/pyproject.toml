[project]
name = "conversation-python-sdk"
version = "0"
description = "Dapr conversation API quickstart (Python SDK)."
requires-python = ">=3.11"
dependencies = [
    "dapr>=1.18.0",
]

[tool.uv]
package = false
