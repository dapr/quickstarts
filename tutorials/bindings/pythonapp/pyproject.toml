[project]
name = "bindings-tutorial-pythonapp"
version = "0"
description = "Dapr bindings tutorial (Python app)."
requires-python = ">=3.11"
dependencies = [
    "requests",
]

[tool.uv]
package = false
