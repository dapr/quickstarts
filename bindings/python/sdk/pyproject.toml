[project]
name = "bindings-python-sdk"
version = "0"
description = "Dapr bindings quickstart (Python SDK)."
requires-python = ">=3.11"
dependencies = [
    "dapr>=1.18.0",
    "Flask",
    "uvicorn",
    "typing-extensions",
    "werkzeug>=3.0.3",
]

[tool.uv]
package = false
