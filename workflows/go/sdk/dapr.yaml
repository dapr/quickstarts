version: 1
common:
  resourcesPath: ../../components
   daprdLogDestination: console
   logLevel: debug
apps:
  - appDirPath: ./order-processor/
    appID: order-processor
    command: ["go", "run", "."]

