[project]
name = "workflows-python-sdk"
version = "0"
description = "Dapr workflows quickstart (Python SDK)."
requires-python = ">=3.11"
dependencies = [
    "dapr>=1.18.0",
    "dapr-ext-workflow>=1.18.0",
]

[tool.uv]
package = false
